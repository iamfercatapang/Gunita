# Copy this file to `.env.local` and fill in your own values.
# `.env.local` is gitignored.

# Google Drive OAuth Client ID (web application).
# Get this from console.cloud.google.com → APIs & Services → Credentials.
# Add your dev origin (http://localhost:5173) and prod origin to Authorized JavaScript Origins.
VITE_DRIVE_CLIENT_ID=
